upstream backend {
    server {{ backend_server }};
}

server {
        listen   80;
        server_name   {{ server_name }};
        root /var/www;

        access_log  /var/log/nginx/{{ log_prefix }}_http.log;
}
